// pas de fonction générée
// donc pas accès à `exports`, `require`, `module`, `__filename`, `__dirname`

export function sum(a, b) {
  return a + b;
}

export function multiply(a, b) {
  return a * b;
}
